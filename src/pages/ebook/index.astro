---
import BaseLayout from "../../layouts/BaseLayout.astro";
import BrandMark from "../../components/BrandMark.astro";

const title = "Ebook — Øui";
const description = "Un cadre éditorial dense. À rouvrir quand il faut décider.";
---

<BaseLayout title={title} description={description} bodyClass="ebook">
<section class="hero stack article">
  <header>
    <h1>
      Ebook
    </h1>
    <p class="article__desc">
        Aucun chapitre ne suffit seul.
        L’ordre compte.
    </p>
   </header>
</section>
  
<section class="hero stack">

    <div class="intro-lines intro-lines--indent">
      <p class="intro-line">Un cadre éditorial dense.</p>
      <p class="intro-line">À rouvrir quand il faut décider.</p>
    </div>
  </section>

  <section class="grid" aria-label="Ebook Øui">
    <!-- Colonne 1 : contenu -->
    <div class="col content">
      <p class="outline-title">Ce que l’ebook traversera :</p>

      <ul class="moments">
        <li>Être nommé</li>
        <li>Relier</li>
        <li>Laisser une trace</li>
        <li>Devenir retrouvable</li>
        <li>Être associé</li>
        <li>Être attendu</li>
        <li>Se retirer</li>
        <li>Voir l’impact échapper à l’intention</li>
      </ul>

      <div class="divider" aria-hidden="true"></div>

      <div class="exit">
        <p>Il n’y a rien à attendre ici.</p>
        <p>
          Lorsque l’ebook sera disponible,<br />
          il le sera sans annonce.
        </p>
        <p class="link">
          <a href="/textes">Continuer à lire les textes</a>
        </p>
      </div>
    </div>

    <!-- Colonne 2 : état -->
    
    <div class="col state">
      <p class="state-title">Non disponible.</p>

      <p class="state-text">
        L’ebook Øui est en cours de finalisation.<br />
        Il ne sera publié que lorsqu’il sera suffisamment fermé.
      </p>

      <p class="state-text">
        Øui n’est pas un contenu à consommer.<br />
        C’est un cadre à affronter.
      </p>

      <p class="state-text">
        Tant que sa forme n’est pas stable,<br />
        il n’est pas mis à disposition.
      </p>

      <div class="divider" aria-hidden="true"></div>

      <p class="state-text">
        C’est ici qu’Øui s’arrête.
      </p>
    </div>
  </section>

  <style>
    .head { margin-bottom: 1.5rem; }
    .h1 { margin: 0 0 .5rem; display: grid; gap: .25rem; }
    .kicker { font-size: .95em; opacity: .7; font-weight: 650; }
    .name { font-size: 1.35em; font-weight: 750; }
    .subtitle { margin: 0; opacity: .8; max-width: 70ch; }

    /* Grid */
    .grid{
      margin-top: 2rem;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 2rem;
      align-items: start;
      max-width: 100%;
    }
    @media (max-width: 900px){
      .grid{ grid-template-columns: 1fr; gap: 1.5rem; }
    }

    .col{ max-width: 70ch; }

    /* Colonne état */
    .state{
      border: 1px solid rgba(0,0,0,.12);
      border-radius: 14px;
      padding: 1rem;
      margin-top:var(--s5);
    }
    .state-title{ margin: 0 0 .5rem; font-weight: 800; }
    .state-text{ margin: .75rem 0 0; }

    /* Colonne contenu */
    .outline-title{ margin: 0 0 .75rem; font-weight: 650; }

    .moments{
      list-style: none;
      padding: 0;
      margin: 0;
      border-top: 1px solid rgba(0,0,0,.10);
    }
    .moments li{
      padding: .35rem 0;
      border-bottom: 1px solid rgba(0,0,0,.);
    }

    .order{ margin-top: 1rem; font-weight: 650; }

    .divider{
      margin: 1.25rem 0;
      height: 1px;
      background: rgba(0,0,0,.10);
    }

    .exit{ opacity: .85; }
    .exit p{ margin: .5rem 0 0; }
    .link a{
      text-decoration: none;
      font-weight: 650;
      border-bottom: 1px solid currentColor;
    }
  </style>
</BaseLayout>
